<div class="md:flex md:flex-wrap-reverse md:justify-between md:items-center md:space-x-2">
  <div class="pb-4 flex-grow">
    <%= form_for(questions_path, method: :get) do |f| %>
      <%= text_field_tag :q, params[:q], placeholder: "Search", class: "w-full max-w-xl" %>
    <% end %>
  </div>
  <div class="pb-4">
    <%= pagy_prev_link(@pagy, html: { class: "button" }) %>
    <%= pagy_next_link(@pagy, html: { class: "button" }) %>
  </div>
</div>
